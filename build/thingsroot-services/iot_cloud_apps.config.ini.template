[mqtt]
host=${MQTT_HOST}
port=${MQTT_PORT}
keepalive=${MQTT_KEEPALIVE}
user=${MQTT_USER}
password=${MQTT_PASSWORD}
clientid_base=IOT_CLOUD_APPS

[influxdb]
host=${INFLUXDB_HOST}
port=${INFLUXDB_PORT}
username=${INFLUXDB_USER}
password=${INFLUXDB_PASSWORD}

[redis]
url=${REDIS_URL}

[iot]
url=${FRAPPE_URL}
auth_code=${FRAPPE_AUTH_CODE}
